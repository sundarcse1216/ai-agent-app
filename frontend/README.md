# frontend

React + TypeScript + Vite SPA for the Smart AI Multi-Agent Assistant. See the [repo root README](../README.md) for setup, architecture, and how this talks to the backend.

```bash
npm install
npm run dev      # dev server on :5173, proxies /api to the backend on :8000
npm run build    # production build to dist/
```
